import { GetJsonState } from "ReadState";
import { Script } from "vm";

const GetJsonState = require('jsonState');

// Call the GetJsonState function
const jsonState = GetJsonState();
console.log(jsonState);


function Home() {
  return (
    <h1>as</h1>
  )
}

export { Home };
